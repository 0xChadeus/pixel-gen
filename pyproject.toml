[project]
name = "pixel-gen"
version = "0.1.0"
description = "True pixel art generation with EDM diffusion + Aseprite plugin"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
